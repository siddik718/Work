#include <bits/stdc++.h>
using namespace std;
using ll = long long;
const int mod = 1e9+7;
#define all(var) var.begin(),var.end()
void solve(){
    int n;cin>> n;
    vector<int> a(n);
    for(auto &i : a)cin>>i;
    
}
int main(){
    cin.tie(0)->sync_with_stdio(0);
    int t;cin >> t;
    while(t--)
    solve();
    return 0;
}
