#include<bits/stdc++.h>
#define rep(i,a,n) for(ll i = (a); i <= (n); i++)
using namespace std;
using ll = long long;
void solve(){
}
int main(){
    #ifndef ONLINE_JUDGE
    freopen("test.in", "r", stdin);
    #endif
    ios::sync_with_stdio(false);
    int test;cin >> test;
    while(test--)solve();
    return 0;
}